name: ⚙️ Backend CI/CD (Microservices)

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/backend-ci.yml"
      - "backend/**"
  pull_request:
    branches: [main]
    paths:
      - "backend/**"

jobs:
  get-services:
    runs-on: ubuntu-latest
    environment: DEV
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Cache Node modules backend
        uses: actions/cache@v3
        with:
          path: backend/node_modules
          key: ${{ runner.os }}-node-backend-${{ hashFiles('backend/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-backend-

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: ./backend
        run: npm install

      - name: Lint code
        working-directory: ./backend
        run: npm run lint

      - name: Format check
        working-directory: ./backend
        run: npm run format -- --check
      
      - name: Start Database Services (Docker Compose)
        working-directory: ./database
        run: docker compose up -d

      - name: Wait for Database to be Ready
        run: sleep 10

      # --- BƯỚC ĐÃ TỐI ƯU HÓA (KẾT HỢP LOAD SECRETS VÀ PRISMA GENERATE) ---
      - name: Load Secrets, Generate Prisma Client, and Build
        working-directory: ./backend # Áp dụng cho cả 3 lệnh
        shell: bash
        run: |
          echo "::group::1. Load Database Secrets"
          # Ghi Secret vào file tạm (phương pháp đáng tin cậy nhất)
          cat << EOF > secret_config.json
          ${{ secrets.DATABASE_CONFIG }}
          EOF

          # Tăng cường bảo mật: Thêm Masking cho toàn bộ Secret
          echo "::add-mask::${{ secrets.DATABASE_CONFIG }}"

          # Chuyển đổi JSON thành chuỗi export (export KEY='VALUE';)
          ENV_EXPORTS=$(jq -r '
            to_entries | 
            .[] | 
            # Đảm bảo dùng single quotes để giá trị không bị hiểu sai bởi shell
            "export \(.key)='\''\(.value)'\'';"
          ' secret_config.json)

          # Thiết lập biến môi trường vào shell hiện tại
          eval $ENV_EXPORTS

          # Dọn dẹp file tạm
          rm secret_config.json
          echo "::endgroup::"

          # ----------------------------------------------------
          echo "::group::2. Create Database"
          echo "Checking and creating databases..."
          ADMIN_DB_URL="postgresql://postgres:postgres@postgres:5432/postgres"

          DB_NAMES=("user" "notification" "task")

          for db_name in "${DB_NAMES[@]}"; do
            echo "Attempting to create database: $db_name"
            docker run --rm --network host postgres psql "$ADMIN_DB_URL" -c "CREATE DATABASE \"$db_name\" IF NOT EXISTS" || true
          done

          echo "Database creation attempt complete."
          echo "::endgroup::"

          # ----------------------------------------------------

          echo "::group::3. Generate Prisma Client for each service"
          echo "🔍 Looking for prisma schemas..."

          # Lệnh này bây giờ SẼ THÀNH CÔNG vì các biến môi trường (như DATABASE_URL)
          # đã được thiết lập bởi eval $ENV_EXPORTS
          find apps -type f -path "*/prisma/schema.prisma" | while read schema; do
            service=$(echo "$schema" | cut -d'/' -f2)
            echo "🚀 Generating Prisma Client for $service using $schema"
            npx prisma generate --schema="$schema"
          done
          echo "::endgroup::"

          # ----------------------------------------------------

          echo "::group::4. Build Backend"
          npm run build
          echo "::endgroup::"

          # ----------------------------------------------------

          echo "::group::5. Run Backend"

          for service in apps/*; do
            service_name=$(basename "$service")
            if [ "$service_name" != "api-gateway" ]; then
              echo "🚀 Testing service: $service_name"
              # Start the service in background
              npm run start:dev $service_name &
              PID=$!
              
              # Wait a few seconds to see if it crashes
              sleep 5

              if ps -p $PID > /dev/null; then
                echo "✅ Service $service_name started successfully!"
                kill $PID
              else
                echo "❌ Service $service_name failed to start!"
                exit 1
              fi
            fi
          done
          echo "::endgroup::"

          echo "::group::6. Test Backend"
          npm run test --if-present
          echo "::endgroup::"
